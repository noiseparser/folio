# Search behavior

Global search covers account names, domains, contacts, owners, and deal stages. Exact account-name matches rank first, followed by name prefixes, domains, contacts, and partial text matches.

Search remains scoped to the active workspace and excludes archived accounts by default.
