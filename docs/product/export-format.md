# Account export format

Account exports include the visible portfolio fields in their current filtered order. Values are quoted consistently and spreadsheet formulas are escaped.

Exports contain account data only; private notes and internal activity details require a separate workspace archive.
