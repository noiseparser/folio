# Activity types

The relationship timeline supports notes, meetings, calls, emails, stage changes, proposals, and completed follow-ups.

Events are immutable once recorded. Corrections create a new event so teams retain a trustworthy account history.
